import {StyledSection} from './StyledCommon'

const Print = () => {
    return(
        <StyledSection>
            <button onClick={() => false}>PRINT</button>
        </StyledSection>
    )
}

export default Print